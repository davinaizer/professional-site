interface:
  display_name: "Close Task"
  short_description: "Close a task and prepare the next handoff"
  default_prompt: "Use $complete-task to close the reviewed task, prepare the next-task handoff, and generate a Conventional Commit message."
