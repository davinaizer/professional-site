interface:
  display_name: "Review Task"
  short_description: "Review completed work against its approved plan"
  default_prompt: "Use $review-task to review the completed implementation against my approved task plan."
