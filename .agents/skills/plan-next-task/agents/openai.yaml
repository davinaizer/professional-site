interface:
  display_name: "Plan Next Task"
  short_description: "Prepare one scoped task for developer approval"
  default_prompt: "Use $plan-next-task to prepare the next small task for my approval without implementing it."
